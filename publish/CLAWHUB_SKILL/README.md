# ClawJobs Deploy Skill

This ClawHub skill installs, configures, and diagnoses the ClawJobs OpenClaw plugin.

It is not the plugin itself. It is a helper layer for:

- installing the plugin through the official OpenClaw CLI
- writing `plugins.entries.clawjobs.config`
- validating local config
- diagnosing gateway or hub connectivity problems
